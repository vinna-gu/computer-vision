% generate_results.m
function[] = generate_results(filename, reduceAmt, reduceWhat)
% filename = image, reduceAmt = (integer) how many pixels to reduce width/height,
% reduceWhat = width or the height (string)

img = imread(filename);




end